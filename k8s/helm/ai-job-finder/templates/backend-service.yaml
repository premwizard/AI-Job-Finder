apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-job-finder.fullname" . }}-backend
  labels:
    app.kubernetes.io/name: {{ include "ai-job-finder.name" . }}-backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ai-job-finder.name" . }}-backend
