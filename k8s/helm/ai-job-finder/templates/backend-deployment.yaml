apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "Crown Atlas.fullname" . }}-backend
  labels:
    app.kubernetes.io/name: {{ include "Crown Atlas.name" . }}-backend
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "Crown Atlas.name" . }}-backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "Crown Atlas.name" . }}-backend
    spec:
      containers:
        - name: backend
          image: "{{ .Values.backend.image }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          envFrom:
            - configMapRef:
                name: {{ include "Crown Atlas.fullname" . }}-config
            - secretRef:
                name: {{ include "Crown Atlas.fullname" . }}-secret
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          volumeMounts:
            - name: chroma-data
              mountPath: /app/chroma_db
            - name: uploads-data
              mountPath: /app/uploads
      volumes:
        - name: chroma-data
          persistentVolumeClaim:
            claimName: {{ include "Crown Atlas.fullname" . }}-chroma-pvc
        - name: uploads-data
          persistentVolumeClaim:
            claimName: {{ include "Crown Atlas.fullname" . }}-uploads-pvc
