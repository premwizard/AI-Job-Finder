{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "Crown Atlas.fullname" . }}-postgres
  labels:
    app.kubernetes.io/name: {{ include "Crown Atlas.name" . }}-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    app.kubernetes.io/name: {{ include "Crown Atlas.name" . }}-postgres
{{- end }}
